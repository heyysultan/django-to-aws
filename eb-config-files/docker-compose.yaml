version: '3'
services:
  web:
    image: heyysultan/django-app
    environment:
      - DEBUG=True
    working_dir: /demo # Set the working directory to the location of manage.py
    ports:
      - "8000:8000"
    # command: ["python", "manage.py", "runserver", "0.0.0.0:8000"]
